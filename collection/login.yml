info:
  name: /login
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_uri}}/login"
  body:
    type: json
    data: |-
      {
        "mail": "luan@mail.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
